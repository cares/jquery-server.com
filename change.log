o ManagementUser.php
oo allow to belong to groups
oo create new user with roles (admin, user)

2014-06-14_13:14

ok can not test for missing database with $mysqli_object = new class_mysqli_interface();
... now you can. 
like this:

/* check if a given database exists */
function databaseExists($database_name)
{
	$result = false;
	global $mysqli_object; global $worked; $worked = false; global $output; $output = "";
	global $settings_database_name; global $settings_lastDatabase;
	
	$query = "";
	$output = $mysqli_object->query($query,$database_name);

	if(is_string($output))
	{
		$output_array = string2array($output);
		if($output_array["id"] == "db does not exist")
		{
			$result = false;
		}			
	}

	return $result;
}

2014-06-11_20:37